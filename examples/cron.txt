# Nightly at 03:00. cron has a minimal PATH, so spell out where node (nvm) and summarize (Homebrew) live.
# Edit the nvm version and the config path, then:  crontab -e  and paste the line.
0 3 * * * PATH=/opt/homebrew/bin:$HOME/.nvm/versions/node/v24.21.0/bin:/usr/local/bin:/usr/bin:/bin summarize-watch run --config $HOME/watch/watch.yaml >> $HOME/watch/state/cron.log 2>&1
